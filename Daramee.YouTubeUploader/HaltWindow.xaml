<Window x:Class="Daramee.YouTubeUploader.HaltWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Daramee.YouTubeUploader"
        mc:Ignorable="d" ResizeMode="NoResize"
        Title="Microsoft® Windows® 종료" Height="100" Width="320" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="24" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <ProgressBar x:Name="progressToHalt" Maximum="30" ValueChanged="progressToHalt_ValueChanged" />
        <Button Content="취소" Width="60" Grid.Row="1" Margin="0,5,0,0" Click="Button_Click" />
    </Grid>
</Window>
